# TraceLens 설계서 — 살아있는 문서

- 지위: **동결 해제.** v3.1(2026-08-11)까지의 동결 이력은 `2026-08-11-algo-scope-design.md`에 보존하고, 이 문서가 현재 기준이다. 변경은 자유롭되 **부록 A 결정 기록에 남긴 것만 결정이다.**
- 팀: 컴퓨터공학 전공 3인 · 최근 전면 개정: 2026-08-18

## 0. 한 문장

> **The trace decides what happened; the director decides what is worth showing; the metaphor decides how it reads.**
> 무슨 일이 일어났는가는 실행이, 그중 무엇을 보여줄지는 연출이, 그것이 어떻게 읽히는가는 은유가 결정한다.

## 1. 제품 정의

**기술적 정의**: Python 코드의 실제 실행 상태를 추적하고, 이를 자동 설명 애니메이션(무비)으로 변환하는 코드 실행 시각화 도구.

**두 축 포지셔닝**:

1. **대표 시나리오 — AI 코드 검증**: AI(ChatGPT·Claude 등)가 생성한 코드를 자기 프로젝트에 적용하기 전에 실행 흐름을 이해하는 용도. 기술적 제약이 아니라 대표 사용 맥락이다 — 사람이 쓴 코드도 동일하게 처리한다.
2. **합격선 — 비전공 초보자의 흐름 이해**: 코딩을 아예 안 해본 사람도 영상만 보고 "무슨 일이 어떤 순서로 일어났는지" 정도는 따라갈 수 있어야 한다. 변수명·인덱스 같은 코드의 기호가 아니라 **물체의 언어**(막대·저울·카드)로 말하는 은유 스킨이 이 합격선에서 나왔다 (5절).

**해결하는 문제**: AI 코드를 이해 없이 복붙하는 습관, 그리고 텍스트 설명만으로는 실행 흐름이 머리에 그려지지 않는 학습자.

**핵심 제품 가설** (실험으로 검증할 대상이며, 전제가 아니다):

> 실제 실행 상태를 시간적으로 시각화하면, 텍스트 설명만 제공하는 것보다 프로그램 상태 변화와 실행 흐름을 이해하는 데 도움이 된다.

**차별화 4축**:

1. **값의 신뢰성** — 화면의 모든 값·순서는 실제 실행에서만 나온다 (코드가 논리적으로 올바르다는 보증은 아니다).
2. **재생 모델** — debugger-first(Python Tutor)가 아니라 narrative-first: 중요한 장면·속도·표현을 골라 자동 재생하는 영화. Debugger → Movie.
3. **영상 그 이상** — 실시간 렌더이므로 언제든 일시정지하고 그 seq의 변수·객체 상태를 인스펙터로 들여다본다.
4. **은유 물체 언어** — 값은 막대로, 판단은 저울로, 반복은 도는 화살로. 은유는 표현만 바꾸고 사실은 트레이스가 소유한다 — "정직한 은유"가 계약이다.

**Core와 Quality의 분리**: Core(제품 완주 조건)는 실제 trace + trace-grounded 필름 + 자동 재생 + 인스펙터 — LLM 없이 규칙 대본만으로 성립한다. Quality layer는 LLM Director가 자막·완급·표현 선택을 보태 설명 품질을 높인다. "왜 AI가 필요한가"는 ablation으로 정량 증명한다 (8절).

## 2. 범위

### 증명 범위 (MVP)

- **지원 코드**: self-contained · single-file · synchronous Python (표준 라이브러리 중심)
- **명시적 범위 밖**: 외부 모듈·DB·네트워크·프레임워크, `input()` 등 interactive, generator/coroutine/async/thread. preflight에서 안내하며 벤치마크 셋에서도 제외 (지원하는 척하지 않는다)
- **렌더러**: SVG + GSAP 단일 (PixiJS 보류 자산은 2026-08-18 의존성째 삭제 — git 이력에만 존재)

### 지원 범위 매트릭스

**A. 완전 지원 — 특화 시각화로 재생**

| 코드 유형 | 예시 | 화면 |
|---|---|---|
| 변수 할당·연산 | `x = a + b` | 변수 알약, 값 변화 플래시 |
| 조건문·반복문 | `if` / `for` / `while` | 비교 저울(참/거짓 도장), 반복 배지·스핀, 루프 압축 빨리감기 |
| 함수 호출·재귀 | `f(x)`, 재귀 | 하단 프레임 카드 (호출 조명) |
| 리스트·dict·set 조작 | `append`, `d["k"]=v`, `pop` | 칸 상자 + 값 막대(숫자), 값 이동 칩, swap 애니메이션 |
| **2차원 프림 리스트 (maze·DP)** | `[[0,1],[1,0]]`, `["S.#", …]` | **격자 렌더러** — 벽·방문 칠·커서·경로 선 (v3.1의 B에서 A로 승격, 2026-08-18) |
| aliasing·얕은 복사 | `b = a`, `b = a[:]` | 상자 이름표 "a · b" (같은 상자에 이름 두 개 — 끈·화살표는 폐기) |
| 예외 발생 | `arr[5]` → IndexError | 터지는 지점까지 재생 + 빨간 오류 스트립 (콘텐츠로 취급) |
| 클래스 인스턴스 기본 · 컴프리헨션 · print | `p = Point(1,2)`, `print` | 객체 상자 · 시퀀스 · 출력 바 |

**B. 지원 — Generic State View** (트리·그래프 등 복잡 구조, 깊은 중첩 — 표 형태 안전망)
**C. 실행되지만 제한** (긴 실행·무한 루프·대량 데이터 — 상한까지 수집 후 "여기까지 시각화")
**D. 범위 밖 — preflight 안내** (input · generator/async/thread · 네트워크·파일 · 외부 라이브러리 · 조각 코드 · 정의만 있고 호출 없음)

preflight는 best effort — D를 놓치고 실행에 들어가도 런타임 에러 처리로 안전하게 내려온다.

### 확장 경로의 지도 — "범위 밖(D)"의 분해

D행은 한 덩어리가 아니다. 성격이 다른 네 가지 한계의 묶음이고, 그중 셋은 시간 문제다. 매트릭스는 "영원한 불가 목록"이 아니라 **착수 순서표**로 읽는다:

| 한계의 종류 | 해당 유형 | 해소 경로 | 판정 |
|---|---|---|---|
| **① 실행 환경** — 브라우저 Pyodide의 제약 | 네트워크·파일/DB·외부 라이브러리·매우 긴 실행 · `input()` | 서버 실행기(하이브리드 백엔드) + stdin 큐 UI | 백엔드 도입 시 **A~C행으로 이동** — 시각화 문제가 아니었다 |
| **② 관측 어휘** — TraceEvent에 그 사건을 담을 어휘가 없음 | generator(yield/resume) · async/await · thread | 트레이서 의미론 확장 + 유형별 새 은유 부품: 일시정지되는 작업대(generator), 제어권 바통(async), 수영 레인(thread), 배낭 멘 함수(클로저) | **착수 순서의 문제** — 유형마다 [어휘 + 은유 + 정직성 가드] 세트가 필요한 연구 과제 |
| **③ 정직성** — 인터프리터 밖은 관측 자체가 불가 | C 확장·내장 내부의 '과정' (`np.dot`, `list.sort`의 진행) | before/after만 정직하게 보여주거나, **시뮬레이션 모드**를 별도 화면 계약("실행 기록이 아니라 교육용 재연")으로 분리 | **유일하게 본질적인 벽.** 값의 신뢰성 축과 절대 섞지 않는다 |
| **④ 재생 모델** — "완결된 실행의 무비"라는 전제 | 서버 루프·GUI 등 끝나지 않는 실행 | 영화가 아니라 **라이브 방송**(실시간 스트리밍 시각화) — 다른 제품 형태 | 별도 제품 결정이 필요한 영역 |

**인지 한계 노트**: 대량 데이터(10만 원소)·깊은 재귀·스레드 인터리빙은 "그릴 수 없는 것"이 아니라 "**압축 은유가 필요한 것**"이다. 루프 빨리감기와 격자 승격이 이미 이 계열의 해법이었고, 히트맵·분포 요약·대표 구간 샘플링·수영 레인이 다음 후보다.

**심사 답변 문구**: "왜 generator는 안 되나요?" → "안 되는 것이 아니라 관측 어휘 확장의 착수 순서 문제입니다. 본질적으로 불가능한 것은 둘뿐입니다 — 관측 없이 정직하게 보여주기, 그리고 끝나지 않는 실행의 영화."

## 3. 시스템 아키텍처

```
코드 붙여넣기
 → Tracer      Pyodide(Web Worker) 실행 + 상태 스냅샷·비교 → TraceEvent 스트림
 ├─ 필름 트랙 (Core — AI 없이 완주)
 │   → buildStage   캐스팅 — 무대에 올릴 객체·변수·프레임, 격자 판정
 │   → choreograph  안무 — 사건을 모션·자막·값 이동·비교 echo로 번역 (의미층)
 │   → compose      구성 — 샷마다 누가 어디에 얼마나 크게 + 오토 프레이밍 카메라
 │   → WorldStage   SVG+GSAP 렌더 — 은유 부품(막대·저울·칩·격자·플래시)
 └─ 대본 트랙 (Quality — 실패해도 영화는 이미 완성)
     → Digest       프레임 필터링·루프 접기 → DigestSpan
     → Director     LLM이 코드+Digest로 대본 생성 (검증 + 결정적 Resolver)
     → decorate     대본의 완급·연출 동사·staging을 필름 샷에 반영
```

**성격 규정**: Execution-grounded / Non-generative. 같은 트레이스가 주어지면 필름 트랙의 출력은 결정적이다.

**실행 위치**: 실행·시각화는 전부 브라우저. Director 호출 시에만 코드+Digest가 외부 LLM으로 전송 — 사용자 고지·opt-out(local-only)·규칙 폴백을 제품에 반영 (설정 화면 구현됨).

### 모듈 경계

| 모듈 | 책임 | 입력 → 출력 |
|---|---|---|
| Tracer (`src/trace/`) | 실행·스냅샷·델타 합성·chunk flush | 코드 → TraceEvent[] |
| buildStage (`src/film/`) | 캐스팅·격자 판정·수명 | TraceEvent[] (+staging 힌트) → StagePlan |
| choreograph | 사건 → 모션·자막·값 이동 (의미층) | TraceEvent[] + StagePlan + 코드 → Shot[] |
| compose | 구성·카메라 (연출층의 배치) | Shot[] + StagePlan → Composition[] + Camera[] |
| theme | 테마 감지 (정렬/공간/중립) + 막대 대상 판정 | StagePlan + Shot[] → FilmTheme |
| WorldStage | GSAP 타임라인 조립·SVG 렌더 | 위 전부 → 재생 가능한 무대 |
| Digest (`src/digest/`) | 압축·요약·스팬 매핑 | TraceEvent[] → Digest |
| Director (`src/director/`) | LLM 대본 + 검증 + Resolver 치환 | 코드 + Digest → Screenplay |
| 규칙 대본 (`src/screenplay/`) | LLM 없는 폴백 대본 | TraceEvent[] → Screenplay |
| decorate | 대본 → 필름 완급·카메라·스포트라이트 | Shot[] + Screenplay → Shot[] |
| Player (`src/film/useFilm` + UI) | 재생·스크럽·배속·챕터·인스펙터 | 타임라인 → 재생 경험 |

### 보안 — untrusted code 위협 모델 (유지)

사용자는 정의상 이해 못 한 코드를 실행하러 온다. Worker에 애플리케이션 secret을 두지 않고, CSP/connect-src로 실행 컨텍스트의 네트워크를 제한하며, LLM 키는 서버 측 보관이 원칙(현재 미구현 — 9절). "서버에서 직접 실행하지 않으므로 서버 측 RCE 위험·인프라 비용을 크게 줄인다"라고 표현한다 ("보안 문제 제거"라고 주장하지 않는다).

## 4. 데이터 계약

### TraceEvent (구현 확정형)

```
{ seq, kind: line | call | return | exception,
  frameId, parentFrameId, func,
  causedByLine, observedAtLine,
  localsDelta:  [ { name, op: set | delete, value } ],
  objectsDelta: [ { op: set | delete, obj | id } ],
  stdout, error?, returned? }   // returned = return 이벤트의 반환값 (Value)
```

- **귀속 규칙**: 보여주는 것은 "모든 mutation"이 아니라 "source-line boundary에서 관측되는 상태 변화"다. diff는 직전 실행 줄(causedByLine)의 효과로 귀속한다.
- mutation은 별도 kind가 아니라 **objectsDelta로 합성**된다 (v3.1의 `mutation` kind·`reachableObjectsDelta`·`function` 표기는 각각 삭제·`objectsDelta`·`func`로 구현 확정).
- **returned**: `sys.settrace`의 `return` 이벤트가 주는 반환값. 지역변수와 같은 objects 레지스트리로 직렬화하므로 컨테이너 반환도 ref로 해석된다. **예외 unwind와 `None`은 싣지 않는다** — 전자는 `arg=None`이라 `return None`과 구분되지 않고(프레임 플래그로 가른다), 후자는 화면에 올려도 가르치는 게 없다.
- serializer는 observationally passive — repr/getattr 부작용 차단, 불확실 객체는 축약.

### DigestSpan

`{ spanId, sourceSeqRange, lines, eventKinds, funcs, changedVars, iterations?, stdoutDelta?, exception? }` — Digest 요약 단위가 원본 트레이스의 어느 구간인지 고정하는 안정 참조.

### Screenplay (구현 확정형)

```
{ chapters: [ { title, scenes: [
    { seqStart, seqEnd,            // LLM은 spanRef만 고르고 Resolver가 치환한 결과
      primitive, focus, repeat?,
      pacing: slow | normal | fast,
      direction: (zoom | hold | skip)[],   // 연출 동사 — 대상이 없다. 대상은 엔진이 사실에서 찾는다
      narration: { template, bindings } } ] } ],
  staging?: { grid: string[], noGrid: string[] } }   // AI의 표현 '선택' — 판정·좌표는 도구가
```

- **spanRef 원칙 유지**: LLM은 seq 숫자를 생성하지 않는다. 검증(스키마→참조 실존→Resolver) 불통과 시 재시도 후 규칙 폴백.
- **narration 신뢰 구분 유지**: 관찰형(값·상태 — 템플릿+바인딩)과 해석형(역할·의도 — 챕터 제목·요약 한정).
- v3.1의 `camera` 필드는 **연출 동사 direction으로 대체**, `fastForward(배속)`는 `fast`로 확정. `staging`은 08-18 신설.

### Shot / Motion (필름 계약 — 08-18 신설)

```
Shot   { seq, motions: Motion[], durationMs, focus, timelapse?, caption? }
Motion — 의미층의 동사들:
  setVar · enterVar · exitVar · bind(alias) · enterObj · exitObj
  grow · setCell · shrink · shiftLeft · swap · label
  compare { text, targets, a·op·b·verdict }     // 저울의 데이터
  travel  { from, to, text }                    // 값의 이동 (칸↔알약, 라인 접지)
  sortedSweep · loop · loopEnd · spotlight · camera
  pushFrame · popFrame · stdout · raise · shake
  gridCell · gridVisit · gridUnvisit · gridCursor · gridTrail
```

- **caption**: 모든 샷은 모션에서 결정적으로 생성된 학습자 자막 한 문장을 싣는다 — 화면과 자막은 원리적으로 일치한다 ("한 샷 = 한 문장").
- **travel·compare·sortedSweep의 정직성**: 소스 라인 접지로 해석될 때만 발화하고, 해석 실패는 침묵. sortedSweep은 실제로 오름차순으로 끝났을 때만.

## 5. 시각 언어 — 은유 물체의 무대

### 은유 부품 (사실은 트레이스, 은유는 스킨)

| 개념 | 은유 | 정직성 가드 |
|---|---|---|
| 값 | **값 토큰** — 칸에 상주하는 막대(값 비례)+글자 묶음. 교환·이동 때 칸이 아니라 토큰이 난다 (비행 중 슬롯은 비어 보인다). 한 칸 삭제(pop(0)·del)면 뒤 토큰들이 미끄러져 빈 칸을 메운다 | 막대는 음수 없는 전부-숫자 리스트만 — \|v\| 인코딩이 순서를 속일 상황이면 포기. 시프트는 배열 모양이 정확히 "한 칸 빠짐+당겨짐"일 때만 |
| 변수 | 이름표 달린 알약 (ref는 "→ 상자") | "값이 든 상자" 은유는 별칭에서 깨지므로 이름표 모델 유지 |
| 비교 | **저울** — 양팔 값 카드, 무거운 쪽으로 기울고 참/거짓 도장. 비교 중인 칸들 바로 위로 내려앉는다 (자리가 없으면 상단 홈 띠) | 접지된 비교만, 그리고 그 비교가 **판단식 전체와 일치할 때만** — `and`로 이어지거나 체인된 조건은 침묵한다 (한 조각에 도장을 찍으면 참이라 해놓고 몸통이 안 도는 샷이 나온다). 판단이 이어지는 동안 무대에 머문다 (깜빡임 금지) |
| 값의 이동 | **칩 비행** (`x=arr[i]`·`append`·`pop`) | 라인 접지 성공 시만, 샷당 1회 |
| 반환 | **반환 칩** — 닫히는 프레임 카드에서 값 칩이 떠서 부모 카드로 내려앉고 사라진다 (HUD 안의 전용 칩 — 카드는 카메라 밖이라 위의 칩과 같은 부품을 못 쓴다) | 트레이스의 `returned`가 있을 때만. 예외 unwind·`None`은 침묵하고, 도착지가 창 밖이면 띄우지 않는다. 부모가 이름으로 갖지 않은 값이므로 칩은 남지 않는다 |
| 반복 | 배지 + 한 바퀴 도는 화살, 회차 카운트업 (`j 반복 3회차`) | 단위가 다른 총계는 표시하지 않는다 (모순 숫자 금지). 범위는 **사용자 소스**의 반복문에서 잡고 회차는 **몸통 실행 횟수를 1부터** 센다 — 다이제스트 압축 스팬은 반복문의 생애가 아니다. 코드를 못 읽으면 배지가 없다 |
| 완성 | **정렬 스윕** — 칸이 차례로 초록 정착 | 실제 정렬로 끝났을 때만 — **같은 원소 구성으로 다른 순서였던 적이 있고** 마지막이 오름차순일 때. 끝이 오름차순인 것은 결과일 뿐 증거가 아니다 (쌓기만 한 리스트·안 바뀐 리스트는 침묵) |
| 별칭 | 한 상자에 이름표 여러 개 ("a · b") | — |
| 공간 탐색 | 격자 + 방문 칠 + 커서 + 경로 선 | 좌표는 트레이스의 튜플·set·list에서만 |

### 색 4역할 계약

**읽기/주목 = 파랑(accent)** · **쓰기/변화 = 호박(write-flash)** · **참/완성 = 초록(ok)** · **거짓/오류 = 빨강(stop)**. 같은 역할은 같은 색, 다른 역할은 다른 색 — 색이 곧 문법이다 (`src/ui/app.css`에 계약 주석).

### 연출 원칙

- **주인공은 중앙을 지킨다** — sticky focus. 구성(compose)이 배우 가시성의 단일 소유자이고, 다른 객체가 이야기를 가져갈 때만 물러난다. 살아있는 변수는 사라지지 않고, **리드 객체는 살아있는 동안 무대에서 내려가지 않는다**(LINGER는 조연의 규칙이다). 커튼콜은 생존 전원의 최종 상태다.
- **한 값은 화면에 한 번만** — 알약이 접히면 그 값을 다른 부품이 든다. 상자를 쥔 변수는 상자 이름표가, 이름이 겹친 프레임(재귀)의 변수는 그 프레임 카드가 대신 말한다. 접기 판정은 사실이므로 의미층(choreograph)이 소유하고, 구성·렌더는 받아 쓴다.
- **한 샷 = 한 문장** — v3.1의 "동시에 한 동작만"을 대체. 캐스케이드(리터럴 탄생)·구성 전환은 병렬이되, 샷의 의미는 자막 한 문장으로 수렴해야 한다.
- **원인 → 결과 순서** — 값 이동 칩이 내려앉는 순간에 도착지가 갱신되고, swap 샷에는 직전 참 비교가 echo로 머문다.
- **테마는 감지, 불확실하면 중립** — swap≥2·전부 숫자 → 정렬, 격자 존재 → 공간 탐색, 그 외 중립 (`theme.ts`). 감지가 틀려도 값은 틀리지 않는다.
- **자리는 비운 뒤에 채운다** — 좁은 스트립(알약)과 5칸 창(카드)은 자리를 물려주는 구조다. 한 샷 안에서 정리(퇴장·자리 이동)가 먼저, 등장이 나중이며 겹침은 전환 도중에도 0이어야 한다.
- easing 현행: 등장 back.out, 이동 power2.inOut, 스왑은 두 토큰의 **레인 교차**(몸을 낮춰 위 14 / 아래 7의 비대칭 레인으로 비껴간 뒤 back settle로 펴짐 — 상자 안에 머문다). 타임라인 라벨은 절대 위치 — 샷 경계와 초 단위로 일치 (자막-화면 드리프트 금지).
- **semantic timeline 유지** — 애니메이션 중간에 멈춰도 인스펙터는 특정 seq의 일관된 상태를 보여준다.

### 보조 뷰

프리미티브 5종(변수·호출 스택·시퀀스·참조 그래프·Generic)은 **실행 전 초기 화면과 일시정지 인스펙터의 보조 뷰**로 물러났다. Generic State View는 여전히 "지원 범위 내 최종 안전망"이다.

## 6. 재생 경험

1. 붙여넣기 → Run → 로딩 단계 표시 ("Python 환경 준비 → 실행·기록 → 설명 준비")
2. 자동 재생 — 챕터 진행바, 자막 동기화. **자막 선택 규칙**: 규칙 연출에서는 항상 필름 자막(모션 유래), AI 연출에서는 접힌 반복 구간만 필름 자막이고 나머지는 AI 문장 — 기계적 사실은 필름이, '왜'는 AI가 말한다.
3. 인터랙션: 일시정지 / 스크럽 / 배속 / 챕터 점프 / 휠 줌·드래그. 일시정지 시 인스펙터.
4. 레이아웃: 좌 코드(Monaco, 현재 줄 하이라이트) / 우 무대 — 유지.

## 7. 에러 처리와 실행 제한 (유지)

| 상황 | 처리 |
|---|---|
| 사용자 코드 예외 | 콘텐츠다 — 터지는 지점까지 재생, 흔들림 + 빨간 오류 스트립 |
| LLM 실패·검증 불통과 | 규칙 대본 폴백 (영화는 이미 완성돼 있다) |
| 프리미티브 매핑 실패 | Generic State View |
| 실행 상한 초과 | maxEvents + 타임아웃까지 수집, "여기까지 시각화" (maxSerializedBytes는 9절) |
| 무한 루프 | chunk flush로 수집분 보존 후 종료 |
| 실행 불가 코드 | preflight 안내 + 런타임 에러 2단계 |

## 8. 검증 — 한 것과 남은 것

**수행 완료**: Tracer 스파이크(Go — `2026-08-11-tracer-spike-report.md`) · 기술 벤치마크 1차(`2026-08-11-benchmark-report.md`, 하네스는 `src/bench/`에 상시 회귀) · Slice 1 완주(태그 `slice-1`) · 필름 파이프라인 단위·E2E 회귀 235+.

**남은 것**:
- **라이브 사용 평가** — 영상 파일럿 체계(/demo·소재 6편·녹화 가이드)는 08-19 전면 폐기. 검증은 참가자가 **주어진 낯선 코드를 `/app`에서 직접 실행**해 보고 답하는 방식으로: ① transfer 질문(입력을 바꾸면 결과·참조 예측 — 자기가 쓴 코드는 이미 아는 코드라 이해도 측정이 안 되므로 반드시 주어진 코드) ② 자유 입력 단계에서 고도화 피드백 ③ 오개념 징후 관찰(막대·저울 오독). 상세 설계는 실행 직전에 확정한다
- **Ablation: 규칙 무비 vs LLM 무비** — "왜 AI가 필요한가"의 정량 답변 (미착수)
- **이해도 비교 실험** — 표본 수 선계산 후 설계 확정, transfer question 중심
- 은유 스킨 자체의 오개념 리스크 검증 — 파일럿 서술 답변에서 잘못된 멘탈 모델 징후를 채점 항목에 추가

## 9. 미구현 약속 — 계약은 유지, 구현은 미완

정직하게 명시한다. 아래는 설계 계약에는 있으나 아직 구현되지 않았다:

| 항목 | 계약 | 현재 |
|---|---|---|
| LLM 서버리스 프록시 | 키는 서버 측에만 | 클라이언트 직접 호출(`VITE_GEMINI_API_KEY`) — 개발 임시, 배포 전 전환 |
| maxSerializedBytes | 3중 상한의 하나 | 미구현 (maxEvents + 타임아웃 2중) |
| Director 캐싱 | code+digestHash+schema/prompt/modelVersion 키 | **구현됨** (08-20) — 키는 `hash(모델명 + 프롬프트)`로 세 조각을 한 번에, localStorage LRU 30, 적중은 화면에 표시 |
| interrupt (SharedArrayBuffer + COOP/COEP) | 정상 중단 경로 | 미검증 — Worker terminate + chunk flush로 대체 중 |
| Digest의 state 기반 루프 접기 (interesting iteration 분리) | Digest 책임 | 부분 구현 — 현재는 라인 방문 횟수 기반 접기만 |

## 10. 리스크 (갱신)

| 리스크 | 상태·대응 |
|---|---|
| ~~[구 1순위] Tracer 상태 직렬화~~ | **해소** — 스파이크 Go, 벤치마크·회귀로 상시 감시 |
| Untrusted client code 실행 | 유지 — Worker 무비밀 원칙, 프록시 전환 전까지 키 노출이 실질 리스크 (9절) |
| **[신규] 은유의 오개념** — 어긋난 비유는 잘못된 멘탈 모델을 심는다 | 정직성 가드(막대 음수 금지·스윕 실제만·해석 실패 침묵) + 파일럿에서 오개념 징후 채점 |
| 범용 상태 시각화 커버리지 | 은유 부품 + 격자 + Generic 안전망 + 층화 벤치마크 |
| 코드 외부 전송 | 고지·opt-out·local-only 구현됨 |
| 3인 범위 과대 | 증명 범위 고수, 은유 2단계(행위자·작업대)는 파일럿 결과 후 착수 |

## 부록 A: 결정 기록

v3.1까지의 결정(타겟·범위·LLM 역할·spanRef·narration·Pyodide·narrative-first·SVG 단일·KPI 방침)은 `2026-08-11-algo-scope-design.md` 부록 A에 보존. 이후 결정:

| 일자 | 결정 | 근거 |
|---|---|---|
| 08-11 | 필름 엔진 — 스텝 재생을 모션 연속체(무성영화)로 전환 | 자막 없이도 흐름이 읽혀야 한다 |
| 08-16 | 연출 동사(zoom/hold/skip) — AI는 동사만, 대상은 엔진이 사실에서 | LLM이 좌표·사실을 만들 경로 차단 |
| 08-18 | 연출 무대(compose) — 고정 배치도 폐기, 구성·오토 카메라·이름표 (끈 제거) | 지정석 회로도는 시선을 분산시킨다 |
| 08-18 | staging 힌트 — AI가 격자 표현을 '선택', 판정·좌표는 도구 | 의미 판단은 AI의 자리, 사실은 도구의 자리 |
| 08-18 | 학습자 자막 — 모든 샷이 모션 유래 자막을 싣는다, 배지 총계 삭제, 최종 프레임 정산 | 자막-화면 불일치·모순 숫자는 신뢰를 부순다 |
| 08-18 | **은유 스킨이 유일한 베이스** — 막대·저울·스핀·스윕, 정밀 모드 삭제 | 합격선이 "비전공 초보의 흐름 이해"로 확장 |
| 08-18 | 색 4역할 계약 · 저울 지속(비교 연속 구간) | 색이 문법이 되려면 역할이 고정돼야 한다 |
| 08-18 | PixiJS 보류 자산·프로토타입 유산 삭제 | 죽은 코드는 자산이 아니라 오해다 |
| 08-18 | **기획 동결 해제** — 이 문서를 살아있는 기준으로, 역할 분담은 팀 운영으로 분리 | 실측·파일럿이 기획을 계속 고치는 단계다 |
| 08-19 | **확장 경로의 지도** — D행을 실행/관측 어휘/정직성/재생 모델 4한계로 분해. "모든 파이썬의 2D화"를 방향으로 채택하되, 시뮬레이션 모드는 값의 신뢰성 축과 분리된 별도 화면 계약으로만 | 매트릭스가 착수 순서표가 되어야 백엔드 로드맵과 심사 방어가 선다 |
| 08-19 | **영상 파일럿 체계 전면 폐기** — /demo 화면·픽스처·생성 스크립트·소재 6편·진행 가이드까지 제거. 검증은 **라이브 사용 평가**(주어진 낯선 코드를 /app에서 직접 실행 + transfer 질문 + 자유 입력 피드백)로 방향 확정, 상세 설계는 실행 직전에 | 녹화 영상은 제품이 아니라 별도 제작물이다 — 실제 제품을 쓰게 하고 평가받는 쪽이 실사용에 가깝고 고도화 신호도 함께 얻는다. 단, 자기 코드는 이미 아는 코드라 이해도 측정용 코드는 반드시 제공한다 |
| 08-19 | **연출 문법 실험 — 프리셋 3종** (`src/film/presets.ts`): 1 집중(3Blue1Brown 시선 통제) · 2 물성(HRM·디즈니 질량감) · 3 쫀득(Brilliant 보상 비트). 문법 기계(비화자 디밍 · 예고→행동→여운 비트 · 판단/행동 완급 배수 · 카메라 선행 도착)는 1벌, 프리셋은 파라미터 묶음. 재생 패널 스위처로 팀이 축별(시선/리듬/물성/보상) 채점 → 승자 조합을 기본값으로 박고 스위처 삭제 | 완전 별개 시각화 3벌은 유지비 3배 + 판정 기준 부재. 같은 엔진 위 파라미터 실험이 "래퍼런스 장점만 조합해 우리 것 만들기"의 공학적 형태다 |
| 08-19 | **실험 수렴 + 인덱스 포인터화** — 팀 채점: 리듬=물성 승(채택), 보상=완성 스윕만 생존, 시선·물성은 전패 → 파라미터가 아니라 구조 문제로 판정. 스위처 삭제, 단일 GRAMMAR 고정. 시선의 구조 해법: 소스에 `NAME[IDX]`로 접지된 인덱스 변수는 알약 스트립이 아니라 **그 배열 아래 이름표 화살표**로 산다 (`pointer` 모션 — 코드에 그렇게 쓰였을 때만, 지어내지 않는다). 물성의 구조 해법(값 토큰 지속성)은 다음 수술 | "j=2 알약"과 "3번째 칸"을 머릿속에서 조인시키는 구조가 난잡함의 뿌리 — 위치 변수는 위치로 그려야 한다 |
| 08-19 | **저울 착지** — 저울 자리는 배치의 단일 소유자인 compose가 샷별로 산출(`scales`). cell 타깃 비교면 비교 칸들의 화면 x 중점 위·대상 이름표 띠 바로 위로 내려간다. 겹침 0 계약: 후보 자리의 저울 사각형(허브 기준 상34/하48/좌94/우162 — 하48은 ±10° 기운 접시의 바깥 모서리 실측)이 무대 위 다른 배우 사각형(이름표·칸번호·포인터 띠 포함)과 교차하면 내려가지 않고 **상단 홈 띠로 폴백** (다른 배우 위를 떠도는 저울은 오독). 좌표는 오토 프레이밍 카메라에 **AI 연출 줌(camera 모션, film-cam-auto)까지 겹친 최종 화면 기준** — 브라우저 실측에서 줌 누락이 겹침을 만들었고, 줌 샷의 저울 이동은 줌 트윈과 같은 길이·ease로 동기화한다. 홈 띠(y≈2~79)와 AREA.y0=84 경계 계약은 유지 — 홈은 등장·폴백 위치가 된다. var 전용 비교는 홈 고정, 비교 연속 구간의 사이 샷에도 카메라를 따라 자리 갱신 | 판단(저울)과 대상(칸)이 한 시야에 있어야 시선 왕복이 사라진다. 겹침은 위치 미세조정이 아니라 경계 계약 + 기하 판정(빈 공간 증명)으로 막는다 |
| 08-19 | **값 토큰 (물성 수술)** — 칸의 값 시각(막대+글자)을 `.cell-token` 서브그룹으로 분리. 슬롯(배경·플래시·링·완성칠·번호)은 "자리"로 붙박이, 토큰이 "물건"으로 움직인다. swap은 상자가 아니라 두 토큰이 이동한다(비행 중 두 슬롯은 실제로 비어 보인다), 읽기 이동(`x = arr[j]`)은 원본 토큰이 남고 칩이 토큰 카드로 복제 비행(파이썬 복사 의미론과 일치), 소멸은 슬롯과 토큰이 함께 진다. 읽기 펄스는 슬롯이 아니라 토큰이 하되 swap 동반 샷에서는 생략(transform 경합 지터 제거). **전역 아이덴티티 추적(토큰 재부모화·절대좌표 상주)은 기각** — 구성 전환마다 전 토큰 재트위닝이 필요하고 재부모화는 스크럽(semantic timeline) 계약과 충돌한다. Motion 스키마·choreograph·compose 무변경, WorldStage 렌더 층만 수술 | 물성 전패의 원인은 "텍스트 교체"가 이동감을 못 만드는 것 — 물리적 아이덴티티는 움직임이 보이는 순간(비행)에만 성립하면 지각적으로 등가다 |
| 08-19 | **저울 겹침 0의 완성 — 경계에 진짜 여백을** — 남아 있던 스침의 원인은 셋이었다. ① 대상이 무대에서 내려가 해석이 실패하면 **직전 자리를 검사 없이 재사용**해, 가만히 선 저울 밑으로 다른 배우가 미끄러져 들어왔다 → 해석 실패 시에도 홈에서 다시 빈 자리를 찾는다. ② 구성·카메라 **전환은 애니메이션**인데 정착 상태만 검사했다 → 이 샷의 점유 범위를 [직전 배치 ∪ 현재 배치를 직전 카메라로 본 것 ∪ 현재 정착]의 껍질로 잡는다 (트윈은 두 끝 사이를 지나므로 껍질을 피하면 전 구간이 안전하다). ③ **`AREA.y0`(84)와 저울 바닥(HOME_Y 36 + S_DOWN 48 = 84)이 딱 맞물려 여유가 0**이라, 판정 여유를 주면 저울이 스스로 계약선을 넘어 "가짜 겹침"을 만들고 홈 띠 전체가 사용 불가가 되어 오히려 크게 겹치는 자리로 물러났다 → `AREA.y0`을 100으로 내려 경계에 실제 여백을 만들고 판정 여유(가로·세로 10)를 얹는다. 자리를 못 찾으면 **가장 덜 가리는 자리**로 물러난다 (임의의 자리에 서지 않는다). 검증은 정착뿐 아니라 **샷당 9개 시점**으로 트윈 중간까지 | 겹침 0은 "정착 상태에서 0"이 아니라 "재생 전 구간에서 0"이어야 한다. 그리고 여유는 여유가 있을 때만 줄 수 있다 — 맞물린 경계에 여유를 얹으면 자충수가 된다 |
| 08-19 | **토큰은 슬롯 층 위의 독립 층** — 토큰이 각자의 칸 그룹 안에 살면 SVG 문서 순서가 곧 겹침 순서라, 오른쪽으로 나는 토큰이 **높은 인덱스 칸의 불투명 배경 뒤로 들어간다** (실측: 스왑 33개 순간, 최대 636px² 가림. 왼쪽으로 나는 토큰과 시프트는 나중에 그려져 멀쩡하므로 한쪽만 사라지는 비대칭이 더 어색했다). 렌더를 **슬롯 층(배경·완성칠·플래시·링·번호) 다음에 토큰 층(막대+글자)** 두 패스로 분리한다 — 토큰은 좌표가 이미 상자 로컬 절대값이라 위치는 그대로고, 모든 슬롯 위를 지나간다. 대신 칸의 등장·퇴장·스윕·커튼콜 펄스는 슬롯과 토큰을 **함께** 대상으로 잡아야 한다 (한쪽만 잡으면 자리와 물건이 따로 논다). 런타임 DOM 재정렬은 기각 — 스크럽에서 역방향 복원이 보장되지 않는다. **층 분리의 대가: `smoothOrigin: false` 필수.** 토큰이 독립 요소가 되면 GSAP이 토큰 자신의 bbox로 `transformOrigin: 'center'`를 푸는데, 그 bbox는 글자가 비었다가 값이 채워지며 커진다(중심 32 → 39). smoothOrigin이 그 이동을 "튀지 않게" 보정하면서 **y 오프셋을 영구히 구워버려, 막대와 숫자가 칸보다 9px 아래로 내려앉았다** (실측: 첫 grow 직후 발생, 이후 모든 샷에 잔존. 칸은 −0.6이라 토큰만 어긋나 숫자가 막대 위로 뜬 것처럼 보였다) | "슬롯은 자리, 토큰은 물건"이라는 문장의 정직한 구현은 토큰 층이 실제로 슬롯 층 위에 뜨는 것이다. z-index 없는 SVG에서 겹침 순서는 설계이지 우연이 아니다. 그리고 origin은 요소를 떼어내는 순간 새 함정이 된다 — 기존 규율("칸 scale은 origin을 center로 통일")에 "bbox가 변하는 요소는 smoothOrigin을 끈다"를 더한다 |
| 08-19 | **저울 홈도 안전지대가 아니다** — 홈 띠(y≈2~79)의 안전은 `AREA.y0=84`가 배우를 그 아래에 두는 계약에 기대는데, **AI 연출의 카메라 팬(`camera` 모션)이 콘텐츠를 위로 올리면 그 계약이 깨진다** (실측: 팬 y≈−18.5로 배우 상단이 81까지 올라와 저울 빔과 3px 겹침). 앞선 저울 착지 작업은 cell 후보만 겹침 검사하고 홈은 무조건 안전하다고 가정해 이 경로를 놓쳤다. 수정: **홈 후보도 같은 기하 검사를 받고, 겹치면 좌우로 40px씩 스윕해 빈 자리를 찾는다** (위로는 갈 곳이 없다 — 프레임 상단). 끝내 자리가 없으면 원래 x를 유지한다 (자리를 지어내지 않는다). 회귀 방지는 좌표 고정이 아니라 **비겹침 단언**으로 — 좌표를 박아둔 테스트는 회피 동작을 오히려 실패로 잡는다 | LLM 연출이 성공하느냐(429 폴백이냐)에 따라 화면 계약이 달라지면 안 된다. 규칙 폴백에서만 검증된 겹침 0은 절반짜리 증명이었다 |
| 08-19 | **스왑 = 레인 교차 (포물선 폐기)** — 첫 안의 상하 포물선(−30/+18)은 칸 높이(48)를 넘겨 **토큰이 상자 밖으로 나갔다** (실측 스크린샷: 위 토큰은 이름표 띠·저울 자리까지 올라가고 아래 토큰은 하단 테두리에 걸쳐 렌더 버그로 읽힘). 상하 여유는 구조적으로 없다 — 위는 이름표 띠와 착지한 저울, 아래는 칸 번호가 점유한다. 그래서 진폭을 키우는 대신 **상자 안에서 비껴가게** 바꾼다: 두 토큰이 0.66으로 몸을 낮춰 **비대칭 레인(위 14 / 아래 7)**으로 갈라져 지나간 뒤 back settle로 펴진다 — 대칭 ±11은 글자 디센더까지 포함한 실제 bbox가 하단을 3.8px 넘겨서, 여유가 있는 위로 더 가고 아래로는 덜 내려가되 분리폭(21)은 지킨다. 수용 기준은 눈이 아니라 기하 — **비행 전 구간에서 토큰 사각형 ⊆ 상자 사각형**을 브라우저 실측으로 증명한다. 시프트(직선·원래 크기)와 스왑(레인·축소)의 서명 구분은 유지 | 은유 무대는 부품끼리 자리를 다툰다 — 저울을 칸 위로 내린 결정이 위쪽 공간을 이미 썼으므로, 물성 연출은 남은 공간 안에서 성립해야 한다. 테두리를 가르는 애니메이션은 물성이 아니라 고장으로 읽힌다 |
| 08-19 | **시프트 접지 — pop의 물성 마무리** — 한 칸 삭제로 뒤가 당겨진 모양(next = prev에서 원소 하나 뺀 꼴: 최소 k에서 head 일치 + tail이 prev의 한 칸 뒤와 일치)이면 choreograph가 칸별 setCell+shrink 대신 `shiftLeft { objectId, index, texts }` 하나를 방출하고 샷을 느리게 잡는다(이야기 비트). 렌더 서사: 값이 먼저 떠나고(travel 칩의 출발지를 마지막 칸이 아니라 **빠진 칸 k**로 교정, 원본 토큰은 칩이 뜨는 순간 사라진다 — pop은 복제가 아니라 이동) → 빈 칸이 생기고 → 뒤 토큰들이 한 몸으로 직선 당김(포물선 없음 — 교환의 서명과 구분) → 마지막 슬롯이 접힌다. 착지 순간 내용 교대는 swap과 같은 스크럽 계약. 판정 실패(꼬리 pop·다중 삭제·재배열)는 기존 경로 폴백 — 지어내지 않는다. 값 중복으로 k가 모호하면 최소 k(값이 같아 어느 쪽이든 정직) | pop(0)이 "칸 텍스트 순간이동"으로 남으면 물성 서사가 큐에서 끊긴다 — BFS가 채점 소재다. 삭제 지점은 소스가 아니라 배열 모양의 사실에서 접지한다 (swap 감지와 같은 보수성) |
| 08-19 | **표기 걷어내기 — 화면에서 코드 기호를 뺀다** — 원칙: 화면에 남는 것은 **실행의 사실**이거나 **사용자 코드의 어휘**여야 한다. 타입 이름·0-based 인덱스·호출 괄호는 사실이 아니라 표기 관습이므로 걷어낸다 (값·이름·순서는 그대로 트레이스가 소유하므로 값의 신뢰성 축은 다치지 않는다). ① **타입 글자 삭제** — 상자의 `list`/`dict`/`set`, 격자의 `list 4×4`. "tuple이라는 글자는 아무것도 가르치지 않는다"의 확장이다. ② **칸 번호는 조건부** — 기본 숨김이고 **포인터가 붙은 배열에만** 표시한다. 코드가 실제로 인덱스로 접근할 때만 "j가 가리키는 칸"의 대응이 가르칠 게 있고, 그 외의 번호는 소음이다. ③ **격자 행·열 번호는 유지** — 좌표는 표기가 아니라 공간의 사실이고 자막도 좌표로 말한다. ④ **프레임 카드의 괄호 제거** (`f()` → `f`) — 이름은 코드와의 연결이라 남기고 괄호만 뺀다. ⑤ **자막은 위치를 번호로 부르지 않는다** — 화면이 어디인지 짚어주므로 자막은 무엇이 일어났는지만 말한다. 끝(맨 앞)은 이름이 있으니 부르고 중간은 침묵한다: `numbers[2] = 8` → `numbers 한 칸 = 8`, `2번 칸과 3번 칸이 자리를 바꿉니다` → `두 값이 자리를 바꿉니다`, `0번 칸이 빠지고` → `맨 앞 값이 빠지고`. **서수 번역("세 번째 칸")은 금지** — 화면에 번호가 없으면 검증할 수 없고 코드와 대조하면 어긋난다. AI 문장(Director narration)은 이 결정의 범위 밖이다 | 합격선이 "코딩을 아예 안 해본 사람"인데 화면의 상당 부분이 코드 표기였다. 인덱스 0은 초보의 대표적 걸림돌이고, `list`는 아무것도 가르치지 않는다. 표기를 지우는 것은 사실을 지우는 것이 아니다 |
| 08-19 | **강조(zoom) 재설계 — 대상 위로, 한 비트만, 고르게** — 진단(실측, 버블 39샷): ① **위치** — `cameraFor`가 대상 rect를 쓰지 않고(`_rect`) 프레임 중앙 기준으로 확대한다. 배열은 오토 프레이밍 후 화면 x≈826에 있는데 중앙(600) 기준 1.45배가 되니 중심이 927로 밀리고 오른쪽이 1270까지 나가 **잘린다 — 후보 17개 전부 클리핑**. ② **타이밍** — 샷 시작에 출발해 `d×0.9` 동안 이동하므로 스왑 애니메이션과 겹쳐 카메라가 표류하고, 도착하는 순간 샷이 끝난다 (오토 프레이밍은 `camLead`로 미리 도착하는데 강조만 예외였다). ③ **순서** — 장면 순서대로 예산 3을 소진해 앞쪽에 몰리고, 강조가 장면 **마지막 샷까지 유지**되어 비트가 아니라 상태가 된다. 결정: **(가)** decorate는 `emphasis { k }` 표시만 남기고 **좌표는 배치의 단일 소유자인 compose가 산출**한다(`autos`) — 08-16 "AI는 동사만, 대상은 엔진이 사실에서"의 연장으로, 좌표까지 엔진이 그것도 배치를 아는 층에서. **(나)** 위치는 대상(비교가 짚은 칸 → 스왑 칸 → 포커스 객체)의 **실제 화면 중심을 AREA 중앙**에 놓는다. 확대 후 대상이 AREA에 안 담기면 배율을 낮추고, 그래도 안 되면 **강조를 포기한다** (잘린 강조는 강조가 아니다). **(다)** 타이밍은 `camLead`만큼 먼저 출발해 0.4s에 도착하고 **그 샷 동안 머문 뒤 다음 샷에서 해제** — 장면 끝까지 물지 않는다. **(라)** 순서는 후보를 모아 **최소 6샷 간격으로 최대 3회** (몰림 방지). 수용 기준: 강조 샷의 대상 클리핑 0, 대상 중심이 AREA 중앙 ±40, 간격 ≥6샷·총 ≤3회, 저울 겹침·토큰 가림 회귀 0. **(마)** 후보 판정은 두 층이 같아야 한다 — decorate가 프레임(호출 카드) 포커스를 후보로 인정했는데 compose는 대상으로 안 세어 강조가 조용히 무음이 되는 실측 케이스가 있었다. 호출 카드는 하단 HUD라 카메라 그룹 바깥이고 당겨도 움직이지 않으므로 **양쪽 모두 후보에서 제외**하고, swap을 양쪽에 추가한다. **(바)** 강조 카메라(`film-cam-auto`)의 origin은 리셋과 트윈이 같아야 한다(`'0px 0px'` + `smoothOrigin: false`) — 리셋만 기본값이면 트윈이 origin을 바꾸는 순간 보정 오프셋이 구워져 **배율만 오르고 중심 이동이 어긋난다** (토큰 9px 오프셋과 같은 함정) | 강조는 "무엇이 중요한가"를 AI가 말하고 "그것이 어디 있는가"를 엔진이 아는 구조여야 한다. 배치를 모르는 층에서 좌표를 만들면 대상을 화면 밖으로 밀어내면서도 스스로는 알 수 없다 |
| 08-20 | **정직성 표시 — 화면은 자기가 아는 것만 말한다** — 트레이서는 객체에 `truncated`(20개 초과 절단, 또는 깊이 2 초과로 안 들여다봄)와 `unsupported`(직렬화 불가) 플래그를 달아 보내는데 **필름이 이 둘을 아무 데서도 쓰지 않았다**. 결과: ① 500개짜리 리스트가 20칸만 그려지고도 **온전한 것처럼 보인다** ② 더 심각하게, `sortedSweep`이 보이는 20칸만 검사하므로 **앞 20개가 오름차순이면 "정렬 완성!"을 선언한다** — 전체에 대한 거짓 주장이다. 결정: **(가)** 트레이서가 빌트인 컬렉션에 실제 원소 수 `n`을 싣는다 (`len()`은 list·tuple·set·deque·dict 분기에서만 호출 — 사용자 객체의 `__len__` 부작용 경로를 타지 않는다). **(나)** choreograph가 잘림 상태를 `partial { objectId, shown, total? }` 모션으로 방출한다 (label 모션과 같은 패턴이라 스크럽에 안전). `total`이 없으면 "안을 볼 수 없음" — 불투명 객체와 깊이 절단을 같은 렌더 경로로 처리한다. **(다)** 상자 끝에 중립색(4역할 색 아님) 꼬리표로 `20 / 500`을 적는다. **(라)** **잘린 리스트에는 `sortedSweep`을 주지 않는다** — 일부를 보고 전체를 말할 수 없다. 자막의 "정렬 완성!"도 함께 사라진다 | "지원하는 척하지 않는다"는 범위 매트릭스에만 적용되는 원칙이 아니다. 한 화면 안에서도 아는 것과 모르는 것의 경계가 보여야 한다. 잘린 것을 온전한 것처럼 그리면 값의 신뢰성 축이 무너지고, 그 위에서 내린 판단(정렬 완성)은 거짓이 된다 |
| 08-20 | **Director 캐싱 — 같은 코드면 같은 영화** — 9절이 계약으로 적어둔 미구현 항목(`code+digestHash+schema/prompt/modelVersion` 키)을 구현한다. 문제는 셋이었다: ① 같은 코드를 다섯 번 돌리면 규칙 폴백 3회·AI지만 줌 미선택 2회로 **매번 다른 영화**가 나와 시연이 성립하지 않았다 ② 429가 상시 발생했다 ③ 강조 같은 AI 의존 연출은 **재현이 안 되니 검증도 못 했다**. 결정: **(가)** 키는 `hash(모델명 + buildPrompt(code, digest))` — `buildPrompt`가 이미 코드·다이제스트·지시문(=schema/prompt 버전)을 전부 담고 있으므로 프롬프트 문자열 자체를 해시하면 계약의 세 조각이 한 번에 들어간다. 지시문을 고치면 키가 저절로 바뀐다. **(나)** 해시는 외부 의존 없는 FNV-1a 32비트 2벌(오프셋 다름)을 잇는다 — `crypto.subtle`은 비동기라 배선만 복잡해진다. **(다)** 캐시 대상은 **해석 완료된 `{screenplay, mode}`** (Resolver·검증을 통과한 결과이고, 키에 다이제스트가 들어가므로 그 다이제스트에 대해서만 유효하다). 적중 시 LLM을 아예 호출하지 않는다. **(라)** localStorage에 항목 수 상한(30, LRU)으로 두고 용량 초과는 조용히 무시한다 — 캐시는 있으면 좋은 것이지 필수가 아니다. **(마)** **적중을 화면에 밝힌다** — 상태 표시가 `AI 연출 · 캐시`가 된다. 방금 생성한 것과 저장된 것을 같게 보이면 그것도 화면의 거짓말이다. **(바)** 탈출구로 설정에 **캐시 비우기** 버튼을 둔다 — AI가 한 번 이상한 대본을 뱉으면 그 코드로는 영원히 그 대본이 되므로 **(사)** 캐싱이 곧바로 갚은 빚: 재현 가능해지자 어제 못 잡은 강조 결함 둘이 드러났다 — ① GSAP의 `transformOrigin: '0px 0px'`은 SVG에서 **user space가 아니라 bbox 기준**이라, 안쪽 프레임 카메라가 움직이면 바깥 강조 카메라의 bbox도 밀려 origin이 따라 움직인다(실측: 계산 x=−597인데 실제 −407, **190px 오차**). 두 카메라 모두 `svgOrigin: '0 0'`으로 절대 좌표에 못 박는다. ② 강조 중심 계산이 포인터 화살표 띠를 상자 높이에 안 넣어 대상이 44px 아래로 밀렸다 — 배우의 점유 범위를 이미 아는 `actorRect`를 재사용한다 | 필름 트랙은 이미 결정적인데 대본 트랙이 매번 달라져 최종 화면이 비결정적이었다. 캐싱은 성능 최적화가 아니라 **연출의 재현성**을 만드는 일이고, 재현되지 않는 것은 검증할 수도 시연할 수도 없다. 실제로 캐싱을 넣자마자 어제 "다음 AI 실행에서 확인"으로 미뤄둔 검증이 그 자리에서 끝났다 |
| 08-20 | **카메라는 transform 속성을 직접 쓴다 — GSAP origin에서 손을 뗀다** — 강조 도입 후 "줌 됐다가 화면이 이상한 곳으로 간다"는 증상. 실측으로 두 결함이 나왔다: ① 해제 뒤 카메라가 항등으로 안 돌아오고 **x=185가 남아** 배열이 826에서 1011로 밀린 채 고정됐다 ② `svgOrigin`을 써도 origin이 흔들렸다(`xOrigin`이 0 → 411.8로 바뀌고 smoothOrigin이 그 차이를 x에 구움). 원인은 하나다 — GSAP의 `x/y/scale`은 SVG에서 origin을 거치는데, **안쪽 프레임 카메라가 움직이면 바깥 강조 카메라의 bbox가 따라 밀려 origin이 흔들린다.** `transformOrigin`(bbox 기준)도 `svgOrigin`도 이 상황을 못 버틴다. 결정: **카메라 두 개(`film-cam-frame`, `film-cam-auto`)는 GSAP 프록시를 트윈하고 `transform` 속성을 직접 쓴다** (`translate(x y) scale(k)`). compose가 계산한 값이 곧 행렬이 되어 origin 의미론이 개입할 여지가 없다. 실측: 샷 중간에서 compose 지시와 실제 행렬의 오차 **0**, 강조 전후로 대상이 826 → 600 → 826으로 정확히 복귀. 배우 배치(`[data-obj]`/`[data-var]`)는 기존 GSAP x/y/scale을 유지한다 — 그쪽은 bbox가 안정적이고 이미 검증돼 있다 | origin은 요소를 떼어내거나 부모가 움직이는 순간 함정이 된다 (토큰 9px, 카메라 190px·185px — 같은 뿌리에서 세 번). 값이 곧 행렬이어야 하는 층에서는 추상화를 걷어내는 편이 싸고 정직하다 |
| 08-20 | **강조로 밀려날 배우는 구성이 접는다** — 강조(1.45× push-in)는 대상을 화면 중앙에 놓지만 왼쪽 변수 스트립을 x=−340까지 밀어내, 무대의 26%가 화면 밖이고 **반쯤 잘린 알약이 가장자리에 걸렸다**. 이는 두 원칙을 어긴다: "구성(compose)이 배우 가시성의 단일 소유자"인데 카메라가 그걸 우회해 배우를 실질적으로 지웠고, "살아있는 변수는 사라지지 않는다"가 카메라 때문에 깨졌다. 결정: **강조 샷에서 프레임(0..1200 × 0..640)에 완전히 담기지 않는 배우는 그 샷의 구성에서 뺀다.** 순서는 comps → cams → autos → **강조 샷 구성 정리** → rects/scales이며, cams·autos는 재계산하지 않는다 (재계산하면 강조 좌표가 무효가 되어 진짜 순환이 된다). 대상은 `autoFor`가 AREA(프레임보다 작다) 안에 담기도록 배율을 낮추므로 **절대 접히지 않는다**. 렌더는 손대지 않는다 — WorldStage는 이미 구성에 없는 배우를 페이드 아웃하므로, 사라짐이 카메라의 부작용이 아니라 구성의 결정이 된다. 무대 밖 상태는 인스펙터가 계속 들고 있으므로 사실은 사라지지 않는다 | 반쯤 잘린 배우는 강조가 아니라 사고로 보인다. "이것만 본다"는 선언이라면 나머지는 걸치는 게 아니라 물러나야 하고, 그 결정은 카메라가 아니라 가시성의 소유자가 내려야 한다 |
| 08-20 | **호출 스택은 창(window)으로 본다 — 깊이는 무한, 화면은 유한** — 실측(`fact(10)`): 프레임 카드가 `y = 522 − 68×depth`로 쌓여 **깊이 8부터 화면 위로 나가고**(11장 중 3장 소실), 폭은 `260 − 18×depth`라 깊이 15면 음수가 된다. 더 나쁜 건 **조명 받은(실행 중) 카드가 33샷 중 9샷에서 화면 밖**이라는 것 — 재귀가 바닥을 치고 되돌아오는 가장 중요한 지점이 안 보인다. 이는 "그릴 수 없는 것"이 아니라 2절 인지 한계 노트가 말한 "**압축 은유가 필요한 것**"이다 (루프 빨리감기·격자 승격과 같은 계열). 결정: **(가)** 프레임 카드 배치를 정적 `layout.framePos`에서 **compose의 샷별 결정**으로 옮긴다 — 창은 시간에 따라 움직여야 하므로 정적 배치로는 현재 프레임을 항상 보이게 할 수 없다. **(나)** 슬롯은 5칸(렌더 y 522·454·386·318·250 — 전부 화면 안). 살아있는 프레임이 5개 이하면 깊이순으로 그대로, 넘치면 **가장 깊은 4개**를 위쪽 슬롯에, 맨 아래 슬롯에는 **"… 앞선 호출 k개"** 요약 띠를 둔다 (창이 가장 깊은 쪽을 잡으므로 생략되는 것은 얕은 쪽 = 아래쪽이다). **(다)** 현재 프레임은 가장 깊으므로 **항상 창 안에 있다** — 조명이 화면 밖으로 나가는 일이 구조적으로 없어진다. **(라)** 폭 축소는 창 안 상대 슬롯(0..4)으로만 계산해 음수 폭이 나올 수 없다. **(마)** 스택 높이가 깊이와 무관하게 고정되므로 `layout.height`가 재귀 깊이에 따라 부풀지 않고 HUD 오프셋도 안정된다. 재귀의 지역변수가 알약을 도배하는 문제(실측: `n=9…n=1` 9개)는 **별도 결정으로 미룬다** | 스택은 깊어질 수 있지만 화면은 그럴 수 없다. 잘라야 한다면 어디를 자를지는 정해져 있다 — 지금 실행 중인 곳은 반드시 보이고, 생략된 것은 개수로 정직하게 밝힌다 |
| 08-20 | **재귀의 알약 도배 — 이름이 겹치면 카드가 든다** — 실측(`fact(10)`, 33샷): 알약 스트립에 이름이 전부 `n`인 알약이 최대 8개(9개가 아닌 것은 `MAX_VARS` 상한 때문이고, 넘치는 얕은 프레임의 변수는 이미 **말없이 잘려나가고 있었다**), 정렬이 등장순(`firstTouch`)이라 **"지금의 n"이 맨 아래**에 있으며 이름·색·위치 어느 것도 현재를 구분해주지 않는다. "살아있는 변수는 사라지지 않는다"가 재귀에서 역효과를 낸 케이스다. 결정: 원칙을 **"한 값은 화면에 한 번만 — 알약이 접히면 그 값을 다른 부품이 든다"**로 일반화한다 (기존 "상자를 쥔 변수는 알약을 접는다, 상자 이름표가 대신 말한다"가 그 첫 사례였다). **(가)** 한 이름이 여러 살아있는 프레임에 동시에 있으면 **그 이름을 가진 가장 깊은 프레임의 것만 알약**으로 남고 나머지는 각자의 프레임 카드가 든다. 기준을 "현재 프레임"이 아니라 "가장 깊은 프레임"으로 잡은 이유: 재귀에서는 둘이 같고, 상호 재귀처럼 현재 프레임에 그 이름이 없는 경우까지 덮으며, `Shot`에 새 필드가 필요 없다. **(나)** 이름이 겹치지 않으면 아무것도 접지 않는다 — 범위를 "비현재 프레임 전부"로 넓히면 `total = helper(i) + total` 같은 평범한 중첩 호출에서 바깥 변수 알약이 호출마다 나갔다 들어오는 깜빡임이 생기는데, 신고된 혼란(어느 n인지 모름)은 이름 겹침에서만 나온다. **(다)** 판정은 사실이므로 의미층이 소유한다 — choreograph가 `foldVars { frameId, varKeys, text }` 하나로 방출하고(바뀔 때만 — `label`·`partial`과 같은 스크럽 안전 패턴), compose는 `varKeys`를 알약 후보에서 빼고(`boundVars`·`pointerVars`와 같은 자리), WorldStage는 `text`를 카드 둘째 줄에 쓴다. **두 층이 같은 판정을 따로 하지 않는다** — 강조 후보를 decorate와 compose가 각자 판정해 조용히 무음이 됐던 함정의 재발 방지. **(라)** 카드는 값을 받을 때만 두 줄이 된다 — 이름이 위로 올라가고 값 줄이 켜지므로 재귀 아닌 코드의 카드는 화면 변화가 0이다 (위치는 attr 트윈이라 스크럽에서 양방향 복원된다; 텍스트는 `tl.call`이라 역방향 복원이 없지만 값 줄이 opacity로 함께 꺼지므로 낡은 글자가 보이는 경로는 없다 — 실측 확인). 값 줄은 `n = 9 · i = 3` 형식에 폭이 모자라면 `+k`로 밝힌다 (`20 / 500`·`⋯ 앞선 호출 k개`와 같은 계열). **순서는 알파벳이 아니라 등장 순** — 매개변수는 서명 순으로 들어오므로 폭에 밀려 하나만 남을 때 첫 매개변수가 남는다 (알파벳 순은 `go(n, acc, label)`에서 `acc = 210 +2`를 남기고 정작 `n`을 접었다, 실측). 맞추는 기준은 추정한 글자폭 상수가 아니라 `getComputedTextLength()`의 **실제 렌더 폭**이고, 기준 폭은 **가장 좁은 슬롯**이라 창이 스크롤해 카드가 슬롯을 옮겨도 다시 재지 않는다. 값 줄은 4역할 색이 아닌 중립색 — 물러나 있는 상태이지 지금 일어나는 일이 아니다. **(마)** 창 밖(`⋯ 앞선 호출 k개`)으로 접힌 프레임의 값은 화면에 없다 — 창 결정이 이미 받아들인 손실이고, 요약 띠가 개수로 밝히며 인스펙터가 들고 있다. 부수 효과: unwind에서 알약이 `n = 1 → 2 → … → 10`으로 되돌아오며 카드가 하나씩 사라져, 재귀가 되돌아오는 그림이 알약 하나로 읽힌다. **(바) 자리는 비운 뒤에 채운다** — `fact(10)` 전체 흐름 실측에서 정착 상태는 깨끗한데 **전환 도중** 결함이 드러났다: 퇴장 페이드(0.3s)와 등장 페이드(0.4s)가 **둘 다 샷 시작에 걸려** 있어, 접혀 내려가는 `n = 10` 알약(불투명도 0.96)과 새로 서는 `n = 9` 알약(0.59)이 **3,196px² 겹친 채 둘 다 읽혔다** — 방금 없앤 "어느 n이 지금인가"가 샷의 절반 동안 되살아난 것이다. 같은 뿌리로 호출 스택 창도 앓았다: 창이 스크롤될 때 새 카드가 **직전 점유자가 슬롯을 비우기 전에** 그 자리에 나타나 카드끼리 2,910px², 사라지는 `프로그램` 카드와 들어오는 `⋯ 앞선 호출 k개` 띠가 2,838px² 겹쳤다. 수정: 한 샷 안에서 **정리(퇴장·자리 이동)가 먼저, 등장이 나중**이다. 비우는 데 쓰는 시간을 `min(0.24s, 0.4d)`로 잡고(짧은 샷에서도 등장할 시간이 남는다) 등장은 그 뒤에 출발한다. 이는 배치를 바꾸지 않고 순서만 바꾸므로 저울 겹침 판정의 껍질(직전 ∪ 전환 ∪ 정착)을 넓히지 않는다 — 오히려 동시성을 줄인다 |  같은 이름의 알약 여덟 개는 정보가 아니라 소음이다. 화면이 "지금의 n"을 못 짚어주면 재귀는 트레이스보다 오히려 어려워진다. 그리고 값은 사라지는 게 아니라 **자리를 옮기는 것**이어야 정직하다 — 물러난 값은 그 프레임의 카드가 든다 |
| 08-20 | **반환값을 보이게 한다 — 값은 카드에서 카드로 내려간다** — 앞선 결정으로 재귀의 unwind가 "카드가 하나씩 사라진다"로 읽히게 됐지만, 정작 `fact`가 곱해 올리는 **값 자체가 화면에 없었다**. 원인은 시각화가 아니라 **관측 어휘**였다: `sys.settrace`의 `return` 이벤트는 `arg`에 반환값을 실어 주는데 `tracer.py`가 그것을 버리고 있었다 (2절 확장 경로의 지도 ②의 가장 작은 사례 — 은유가 아니라 어휘가 없어서 못 그린 것이다). 결정: **(가)** 트레이서가 `return`의 `arg`를 `_serialize`해 `returned`로 싣는다. 지역변수와 **같은 `objects` 레지스트리**를 쓰므로 컨테이너를 반환해도 ref가 해석된다. `TraceEvent.returned?: Value`는 선택 필드라 기존 픽스처는 그대로 유효하다. **(나)** 정직성 가드 둘. ① **예외 unwind는 침묵** — 실측(로컬 CPython)으로 확인: 함수가 터지면 `exception` 직후 `line` 없이 `return`이 오고 그 `arg`는 `None`이라 `return None`과 구분되지 않는다. 반면 예외가 그 프레임에서 잡히면 사이에 `line`(except 절)이 낀다. 그래서 프레임별 플래그를 `exception`에서 세우고 `line`에서 지운 뒤, 플래그가 선 채 도착한 `return`은 값을 싣지 않는다 — **없는 반환을 지어내지 않는다**. ② **`None`은 화면에 올리지 않는다** — 암묵 반환과 `return None`은 구분되지 않고, `None`이라는 글자는 초보에게 아무것도 가르치지 않는다 (표기 걷어내기의 연장). **(다)** 의미층이 반환 샷에 `returnValue { frameId, toFrameId, text }`를 방출하고 자막은 `fact가 2를 돌려줍니다`가 된다 (기존 `fact 종료 — 작업 공간이 닫힙니다`보다 그 샷의 사실에 가깝다). **(라)** 렌더는 **기존 "값의 이동 = 칩 비행" 은유를 그대로 쓰되 HUD 안에 전용 반환 칩을 둔다** — 프레임 카드는 카메라 밖(HUD)에 살고 기존 칩은 카메라 안이라, 같은 부품으로는 카드 사이를 날 수 없다. 색은 값의 이동이므로 파랑(accent) — 4역할 계약 그대로다. 칩은 **도착하고 사라진다**: 부모가 그 값을 이름으로 가진 것이 아니므로 카드에 남기면 없는 사실을 지어내는 것이다 (이름이 있으면 다음 샷에 알약으로 뜬다 — `result = 3628800`). **(마)** 도착지가 창 밖이면 침묵하지만, 반환하는 프레임은 정의상 가장 깊고 부모는 그 바로 위이므로 창(5칸) 안에 항상 있다 — 가드는 방어적 무음일 뿐이다. 결과: `fact(10)`의 unwind가 `1 → 2 → 6 → 24 → … → 3628800`으로 읽힌다 | 재귀를 이해한다는 것은 "되돌아오며 값이 만들어진다"를 보는 것이고, 그게 없으면 unwind는 그냥 카드가 사라지는 애니메이션이다. 그리고 이 결함은 "그릴 수 없는 것"이 아니라 **트레이서가 이미 손에 쥐고 있던 사실을 안 실어 보낸 것**이었다 — 매트릭스의 ②가 착수 순서의 문제라는 주장의 가장 값싼 증거다 |
| 08-20 | **"정렬 완성!"의 오판 — 끝이 오름차순인 것과 정렬한 것은 다르다** — 실측: 정렬이 한 줄도 없는 코드(`nums = [1, 2, 3]`을 만들고 읽기만 하는 예외 처리 예제)가 마지막에 `실행 종료 — 정렬 완성!`을 선언했다. 판정이 "마지막 상태가 오름차순인 숫자 리스트(3칸 이상)"이기만 해서 **하지 않은 일을 화면이 선언**하고 있었다. 5절 계약("실제 정렬로 끝났을 때만")보다 구현이 느슨했던 것이고, 잘린 리스트에 스윕을 주지 않기로 한 결정과 같은 계열의 값의 신뢰성 문제다. 결정: 스윕 조건에 **"같은 원소 구성으로 다른 순서였던 적이 있다"**를 더한다 — 정렬은 정의상 **재배열**이므로 원소가 바뀌면 그 이전의 순서는 지금 구성에 대한 증거가 아니다. 판정은 상태 이력 없이 O(1) 메모리로 증분 유지한다: 상자 상태가 올 때마다 원소 다중집합 서명을 만들어 **①** 직전 서명과 다르면 증거 플래그를 지우고 **②** 그 상태가 오름차순이 아니면 플래그를 세운다. 마지막이 오름차순이면서 플래그가 서 있을 때만 스윕과 "정렬 완성!"이 나간다. 이력이 필요 없는 이유: 같은 다중집합에서 오름차순 배열은 유일하므로 **"다른 순서" ⟺ "오름차순이 아님"**이다. 걸러지는 것들: 처음부터 오름차순이고 안 바뀐 리스트 · `append`로 쌓기만 한 오름차순 리스트 · 원소를 갈아끼워 오름차순이 된 리스트(`xs[0] = 0`). 살아남는 것들: 버블·삽입·선택 정렬 · `.sort()` · **쌓은 뒤 정렬**(쌓기가 끝난 시점의 흐트러진 상태가 최종 다중집합과 같으므로 증거가 그때 남는다). 기존 테스트 하나(`잘리지 않았고 실제로 오름차순이면…`)는 느슨한 계약을 박아둔 것이었으므로 재배열 단계를 넣어 고친다 — 그 테스트의 의도는 "절단 가드가 과잉이지 않다"이지 "오름차순이면 무조건 스윕"이 아니었다 | 화면이 "정렬 완성!"이라고 말하면 학습자는 그 프로그램이 정렬을 했다고 배운다. 끝이 오름차순인 것은 정렬의 결과일 뿐 증거가 아니다 — 증거는 재배열이고, 그건 트레이스가 이미 들고 있다 |
| 08-20 | **반복 배지를 소스에 접지한다 — 다이제스트 스팬은 반복문이 아니다** — 실측: `for i in range(4)`(4바퀴)가 배지에 `3 → 4 → 5`로 뜨고 **처음 두 바퀴는 배지가 아예 없다**. 버블 정렬은 `3 → 4 → … → 15`로 중첩된 두 루프가 한 숫자를 공유하며 리셋되지 않고, **정렬 한복판(첫 패스 j=0..3의 한가운데)에서 `반복 구간이 끝났습니다`가 두 번 뜬다**. 원인은 하나다 — 배지가 반복문이 아니라 **다이제스트의 압축 스팬**을 보고 있었다. 접힘이 헤더 3회차 방문부터 시작되니 카운트를 2에서 출발시키는 보정이 들어갔고(그래서 첫 회차가 3이다), 스팬이 끝나면 반복문이 끝난 것으로 읽었다. 화면에 `i = 3` 알약과 `반복 5회차` 배지가 **동시에** 떠서 대조하면 어긋나므로, 이는 08-18이 금지한 **모순 숫자**가 형태만 바꿔 남아 있던 것이다. 결정: **(가)** 반복문의 범위를 **사용자 소스**에서 잡는다 — `for`/`while` 헤더 줄과 들여쓰기 블록의 마지막 줄. 값·순서가 아니라 코드의 모양이므로 "화면에 남는 것은 실행의 사실이거나 **사용자 코드의 어휘**"의 후자다. 판정이 애매하면(한 줄 루프 등) 그 반복문은 목록에서 빠지고 침묵한다. **(나)** 회차는 **몸통이 실행된 횟수**를 1부터 센다 — 헤더 줄을 밟으면 장전하고 몸통 줄에 들어설 때 오른다. 헤더 방문 수를 세면 마지막 소진 검사까지 세어 4바퀴가 5회차가 된다. **(다)** 실행이 그 범위 밖으로 나가면 그 반복문은 끝났고 카운터는 리셋된다 — 중첩에서 바깥이 한 바퀴 돌면 안쪽은 다시 1부터이며, 그게 실제로 일어나는 일이다. **(라)** 배지는 **지금 도는 가장 안쪽 반복문**을 말하고, `for`면 그 변수 이름을 붙인다 (`j 반복 3회차`); 이름을 못 읽으면(`while`·튜플 언패킹) 숫자만. 이름이 없으면 중첩에서 숫자가 `4 → 1 → 2`로 널뛰는 게 근거 없이 보이는데, 이름이 바뀌면 다른 반복문이라는 사실이 읽힌다. **(마)** 반복 안에서 함수를 부르면 배지는 **호출 스택을 거슬러** 살아있는 반복문을 찾는다 — 현재 프레임만 보면 `for x in xs: helper(x)`가 회차마다 배지 켜짐/꺼짐을 반복한다. **(바)** 빨리감기로 접힌 이벤트도 회차는 센다(모션만 내지 않는다) — 안 세면 압축 뒤 숫자가 건너뛴다. **(사)** 코드가 없으면 배지가 없다 — 배지의 근거가 소스이므로 근거 없이 숫자를 만들지 않는다 | 배지는 학습자가 화면에서 "몇 번째"를 읽는 유일한 자리인데, 그 숫자가 바로 옆 알약과 어긋나면 화면 전체의 신뢰가 깎인다. 압축은 보여주는 방식이지 사실이 아니다 — 사실은 소스와 트레이스에 있다 |
| 08-20 | **저울은 조건 전체를 볼 때만 내려온다 — 한 조각에 도장을 찍지 않는다** — 실측(BFS): `if 0 <= nr < 4 and 0 <= nc < 4 and maze[nr][nc] == 0 and (nr, nc) not in visited:`에서 `detectCompare`가 줄의 **첫 비교만** 찾아(`0 <= nr`) 거기에 참/거짓 도장을 찍었다. 체인 비교의 뒤쪽(`< 4`)도, `and`로 이어진 세 절도 보지 않는다. 결과: **32번의 비교 중 21번(66%)이 "참" 도장을 찍고도 몸통이 실행되지 않았다** — 화면이 판단을 선언하고 그 판단과 반대로 움직였다. 결정: **접지한 비교식이 그 줄의 판단식 전체와 일치할 때만 저울을 띄운다.** 판단식은 `if`/`elif`/`while`의 조건이고 대입이면 우변이며, 매치가 그 전체를 덮지 못하면 침묵한다. 저울은 "판단"의 은유이므로 조건의 사분의 일에 접시를 내려놓으면 나머지 사분의 삼을 숨긴 채 결론을 말하는 것이 된다. 단일 비교(`if numbers[j] > numbers[j+1]:` · `if n <= 1:` · `if pos == goal:`)는 그대로이므로 정렬·재귀 시나리오는 손실이 없고, BFS의 큰 `if`에서만 저울이 사라진다 — 그 자리의 주인공은 격자와 방문 칠이다 | 저울의 도장은 화면에서 가장 강한 주장이다. 접지에 성공한 것과 그 접지가 판단 전체를 덮는 것은 다른 문제이고, 후자를 확인하지 않으면 "참"이라 찍은 뒤 아무 일도 일어나지 않는 샷이 3분의 2가 된다 |
| 08-20 | **주인공은 무대를 지킨다 — LINGER는 조연의 규칙이다** — 실측(BFS 124샷): 격자(maze)가 **19샷에서 무대 밖**이고(9개 구간, 최장 116~120의 5샷 연속) **15샷은 상자가 하나도 없어 화면에 알약만 남았다**. `dr`·`dc`만 바뀌는 샷이 이어지면 maze·queue·visited가 전부 `LINGER`(6샷) 만료로 물러나기 때문이다. 공간 탐색의 주인공이 탐색 도중 사라지는 셈이고, 이는 5절의 "주인공은 중앙을 지킨다"가 sticky **포커스**만 보장하고 **체류**는 보장하지 않아 생긴 구멍이다. 결정: **(가)** 리드 객체(`leadObjectId` — buildStage가 변화 횟수·수명으로 이미 고르고 있다)는 살아있는 동안 무대에서 내려가지 않는다. 포커스를 빼앗기면 대기 열로 물러나되 화면에는 남는다. "살아있는 변수는 사라지지 않는다"의 객체판이며, 격자 특례가 아니라 정렬의 배열에도 같은 규칙이 적용된다(버블 정렬은 상시 닿으므로 실질 변화가 없다). **(나)** 명시적 퇴장(`exitObj`)은 그대로 존중한다 — 죽은 상자를 붙잡지는 않는다. **(다)** 무대에 배우가 있는데 포커스가 비면 리드를 포커스로 세운다 — 안 그러면 "무대를 지킨다"가 "구석에서 0.55배로 쪼그라든다"가 되고, 오토 프레이밍이 그 구석을 확대해 화면이 더 이상해진다. **(라) 격자도 함께 상주한다** — 리드만으로는 신고된 증상이 안 고쳐졌다(실측: 리드가 `queue`로 잡혀 상자 0개 샷은 15 → 1이 됐지만 **격자는 그대로 19샷 무대 밖**). `leadObjectId`는 변화 횟수로 고르는데 **미로의 벽은 한 번 만들어지고 안 바뀌므로 격자는 영영 리드가 될 수 없다.** 그러나 공간 탐색에서 무대는 격자 그 자체다 — 변하는 값이 아니라 탐색이 일어나는 공간이고, 그 위의 방문 칠·커서·경로가 전부 격자를 좌표계로 쓴다. 변화 횟수라는 지표가 "무엇이 주인공인가"를 못 재는 유일한 경우라 규칙으로 못 박는다 | 무대에 아무것도 없는 샷이 15개면 그건 연출이 아니라 사고다. LINGER는 "이야기를 가져가지 않는 조연은 물러난다"는 규칙이지 주인공을 내리라는 규칙이 아니었다 |

## 부록 B: 버전 이력

- v1 → v3.1 (2026-08-11): 외부 검토 2회 반영, 기획 동결 — 상세는 `2026-08-11-algo-scope-design.md` 부록 B
- v3.1 → 살아있는 문서 (2026-08-18): 동결 해제. 파이프라인을 필름/대본 2트랙으로 개정, 스키마를 구현 확정형으로 교정(mutation kind 삭제·objectsDelta·direction verbs·staging), 시각 언어를 은유 물체 무대로 전면 개정, 격자 A 승격, 두 축 포지셔닝, 미구현 절·리스크 갱신, 역할 분담 절 제외
